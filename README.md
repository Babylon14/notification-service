# Notification Service (Сервис рассылок)

Сервис для управления контактами и массовой рассылки email-уведомлений с использованием асинхронной архитектуры и очередей задач.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Стек технологий](#стек-технологий)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Установка и настройка](#установка-и-настройка)
- [Запуск проекта](#запуск-проекта)
- [API документация](#api-документация)
- [Миграции базы данных](#миграции-базы-данных)
- [Мониторинг](#мониторинг)
- [Структура проекта](#структура-проекта)

## 🎯 Описание проекта

Notification Service — это REST API сервис для управления контактами и выполнения массовых email-рассылок. Сервис использует асинхронную архитектуру с Celery для фоновой обработки задач отправки писем, что позволяет обрабатывать большие объемы рассылок без блокировки основного приложения.

### Основные возможности:

- ✅ Управление контактами (создание, поиск, удаление)
- ✅ Создание и запуск массовых email-рассылок
- ✅ Асинхронная обработка рассылок через Celery
- ✅ Отслеживание статусов рассылок
- ✅ HTML-шаблоны для писем
- ✅ Автоматические повторные попытки при ошибках
- ✅ Мониторинг задач через Flower

## 🛠 Стек технологий

### Backend Framework
- **FastAPI** (0.128.0) — современный асинхронный веб-фреймворк для Python
- **Uvicorn** (0.40.0) — ASGI-сервер для запуска FastAPI

### База данных
- **PostgreSQL 16** — реляционная база данных
- **SQLAlchemy** (2.0.45) — ORM с поддержкой асинхронных операций
- **Alembic** (1.17.2) — инструмент для миграций базы данных
- **asyncpg** (0.31.0) — асинхронный драйвер для PostgreSQL

### Очереди задач
- **Celery** (5.6.2) — распределенная система очередей задач
- **Redis** (7.1.0) — брокер сообщений и хранилище результатов

### Email
- **aiosmtplib** (5.0.0) — асинхронная библиотека для отправки email
- **email-validator** (2.3.0) — валидация email-адресов

### Мониторинг
- **Flower** (2.0.1) — веб-интерфейс для мониторинга Celery

### Утилиты
- **Pydantic** (2.12.5) — валидация данных и настройки
- **Jinja2** (3.1.6) — шаблонизатор для HTML-писем

### Инфраструктура
- **Docker** и **Docker Compose** — контейнеризация и оркестрация сервисов
- **PgAdmin 4** — веб-интерфейс для управления PostgreSQL

## 🏗 Архитектура

Проект следует принципам чистой архитектуры с разделением на слои:

```
┌─────────────────────────────────────────┐
│         FastAPI Application             │
│  (API Endpoints: /contacts, /mailings)  │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│         Business Logic Layer             │
│  (Services, Repositories, Schemas)       │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│         Data Access Layer                │
│  (SQLAlchemy Models, Database)          │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         Celery Worker                   │
│  (Background Tasks: Email Sending)      │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│         Redis (Message Broker)          │
└─────────────────────────────────────────┘
```

### Компоненты системы:

1. **FastAPI Application** — основной веб-сервер, обрабатывающий HTTP-запросы
2. **Celery Worker** — воркер для выполнения фоновых задач отправки писем
3. **PostgreSQL** — хранилище контактов и информации о рассылках
4. **Redis** — брокер сообщений для Celery
5. **Flower** — веб-интерфейс для мониторинга Celery задач

## 📦 Требования

- **Python** 3.12.8 или выше
- **Docker** и **Docker Compose** (рекомендуется)
- **Git** для клонирования репозитория

Для локальной разработки без Docker:
- PostgreSQL 16+
- Redis 7+

## ⚙️ Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd notification-service
```

### 2. Создание файла конфигурации

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# База данных PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=mailing_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# URL подключения к БД (для приложения)
DATABASE_URL=postgresql+asyncpg://postgres:your_secure_password@db:5432/mailing_db

# Redis
REDIS_URL=redis://redis:6379/0

# PgAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin_password

# SMTP настройки (пример для Mailtrap)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_smtp_user
SMTP_PASSWORD=your_smtp_password

# Безопасность (для JWT, если будет использоваться)
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
```

**Важно:** Замените значения на ваши реальные данные, особенно пароли и SMTP-настройки.

### 3. Настройка SMTP

Для тестирования можно использовать:
- **Mailtrap** (sandbox.smtp.mailtrap.io) — для разработки и тестирования
- **Gmail SMTP** (smtp.gmail.com:587) — для продакшена
- Любой другой SMTP-сервер

## 🚀 Запуск проекта

### Запуск с Docker Compose (рекомендуется)

Это самый простой способ запустить весь стек:

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка всех сервисов
docker-compose down

# Остановка с удалением volumes (удалит данные БД)
docker-compose down -v
```

После запуска будут доступны следующие сервисы:

- **FastAPI API**: http://localhost:8000
- **API документация (Swagger)**: http://localhost:8000/docs
- **API документация (ReDoc)**: http://localhost:8000/redoc
- **Flower (Celery мониторинг)**: http://localhost:5555
- **PgAdmin**: http://localhost:5050

### Запуск без Docker (локальная разработка)

#### 1. Установка зависимостей

```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# На macOS/Linux:
source venv/bin/activate
# На Windows:
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

#### 2. Настройка базы данных

Убедитесь, что PostgreSQL и Redis запущены локально, и обновите `.env` файл:

```env
POSTGRES_HOST=localhost
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/mailing_db
REDIS_URL=redis://localhost:6379/0
```

#### 3. Применение миграций

```bash
alembic upgrade head
```

#### 4. Запуск приложения

В разных терминалах:

```bash
# Терминал 1: FastAPI сервер
uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload

# Терминал 2: Celery Worker
celery -A src.tasks.celery_app worker --loglevel=info

# Терминал 3: Flower (опционально)
celery --broker=redis://localhost:6379/0 flower
```

## 📚 API документация

После запуска приложения автоматически доступна интерактивная документация:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные эндпоинты

#### Контакты (`/contacts`)

##### Создание контакта
```http
POST /contacts/
Content-Type: application/json

{
  "email": "user@example.com",
  "first_name": "Иван",
  "last_name": "Иванов",
  "is_active": true
}
```

**Ответ:**
```json
{
  "id": 1,
  "email": "user@example.com",
  "first_name": "Иван",
  "last_name": "Иванов",
  "is_active": true,
  "created_at": "2024-01-15T10:30:00Z"
}
```

##### Получение списка контактов
```http
GET /contacts/?limit=10&offset=0
```

##### Поиск контакта по email
```http
GET /contacts/search?email=user@example.com
```

##### Удаление контакта
```http
DELETE /contacts/{contact_id}
```

#### Рассылки (`/mailings`)

##### Создание и запуск рассылки
```http
POST /mailings/
Content-Type: application/json

{
  "subject": "Новости компании",
  "content": "Добро пожаловать в нашу рассылку!"
}
```

**Ответ:**
```json
{
  "id": 1,
  "subject": "Новости компании",
  "content": "Добро пожаловать в нашу рассылку!",
  "status": "pending",
  "created_at": "2024-01-15T10:30:00Z"
}
```

**Статусы рассылки:**
- `pending` — рассылка создана, ожидает обработки
- `processing` — рассылка в процессе выполнения
- `completed` — рассылка успешно завершена
- `failed` — произошла ошибка при выполнении

### Примеры использования с curl

```bash
# Создание контакта
curl -X POST "http://localhost:8000/contacts/" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "first_name": "Тест",
    "last_name": "Пользователь"
  }'

# Получение всех контактов
curl "http://localhost:8000/contacts/"

# Создание рассылки
curl -X POST "http://localhost:8000/mailings/" \
  -H "Content-Type: application/json" \
  -d '{
    "subject": "Тестовая рассылка",
    "content": "Это тестовое сообщение"
  }'
```

## 🗄 Миграции базы данных

Проект использует Alembic для управления миграциями базы данных.

### Применение миграций

```bash
# Применить все миграции
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1

# Создать новую миграцию (после изменения моделей)
alembic revision --autogenerate -m "описание изменений"

# Просмотр текущей версии
alembic current

# Просмотр истории миграций
alembic history
```

### В Docker

```bash
# Выполнение миграций внутри контейнера
docker-compose exec app alembic upgrade head
```

## 📊 Мониторинг

### Flower — мониторинг Celery

Flower предоставляет веб-интерфейс для мониторинга задач Celery:

- **URL**: http://localhost:5555

Возможности:
- Просмотр активных задач
- История выполненных задач
- Статистика воркеров
- Мониторинг очередей
- Управление задачами

### PgAdmin — управление базой данных

PgAdmin предоставляет веб-интерфейс для работы с PostgreSQL:

- **URL**: http://localhost:5050
- **Email**: значение из `PGADMIN_EMAIL` в `.env`
- **Password**: значение из `PGADMIN_PASSWORD` в `.env`

Для подключения к БД в PgAdmin используйте:
- **Host**: `db` (внутри Docker) или `localhost` (локально)
- **Port**: `5432`
- **Database**: значение из `POSTGRES_DB`
- **Username**: значение из `POSTGRES_USER`
- **Password**: значение из `POSTGRES_PASSWORD`

## 📁 Структура проекта

```
notification-service/
├── alembic/                    # Миграции базы данных
│   ├── versions/               # Файлы миграций
│   └── env.py                  # Конфигурация Alembic
├── src/                        # Исходный код приложения
│   ├── api/                    # API эндпоинты
│   │   ├── api_contacts.py     # Эндпоинты для контактов
│   │   └── api_mailings.py     # Эндпоинты для рассылок
│   ├── core/                   # Основные настройки
│   │   └── config.py           # Конфигурация приложения
│   ├── models/                 # SQLAlchemy модели
│   │   ├── contact.py          # Модель контакта
│   │   └── mailing.py          # Модель рассылки
│   ├── schemas/                # Pydantic схемы
│   │   ├── schema_contact.py   # Схемы для контактов
│   │   └── schema_mailing.py   # Схемы для рассылок
│   ├── tasks/                  # Celery задачи
│   │   ├── celery_app.py       # Конфигурация Celery
│   │   └── mailing_tasks.py    # Задачи для рассылок
│   ├── repositories/           # Репозитории (для будущего расширения)
│   ├── services/               # Бизнес-логика (для будущего расширения)
│   ├── database.py             # Подключение к БД
│   └── main.py                 # Точка входа FastAPI
├── tests/                      # Тесты (для будущего расширения)
├── alembic.ini                 # Конфигурация Alembic
├── docker-compose.yml          # Docker Compose конфигурация
├── Dockerfile                  # Docker образ приложения
├── pyproject.toml              # Конфигурация проекта Python
├── requirements.txt            # Зависимости Python
└── README.md                   # Документация
```

## 🔧 Разработка

### Добавление новых эндпоинтов

1. Создайте модель в `src/models/`
2. Создайте схемы в `src/schemas/`
3. Добавьте эндпоинты в `src/api/`
4. Подключите роутер в `src/main.py`

### Добавление новых Celery задач

1. Создайте функцию-задачу в `src/tasks/`
2. Декорируйте её с помощью `@celery_app.task`
3. Импортируйте задачу в `celery_app.py`

### Создание миграций

После изменения моделей:

```bash
alembic revision --autogenerate -m "описание изменений"
alembic upgrade head
```

## 🐛 Решение проблем

### Проблема: База данных не подключается

**Решение:**
- Проверьте, что PostgreSQL запущен
- Убедитесь, что значения в `.env` корректны
- Проверьте, что миграции применены: `alembic upgrade head`

### Проблема: Celery задачи не выполняются

**Решение:**
- Убедитесь, что Redis запущен
- Проверьте, что Celery Worker запущен
- Проверьте логи воркера: `docker-compose logs worker`

### Проблема: Письма не отправляются

**Решение:**
- Проверьте SMTP настройки в `.env`
- Убедитесь, что SMTP сервер доступен
- Проверьте логи воркера на наличие ошибок
- Для Mailtrap убедитесь, что не превышен лимит писем

### Проблема: Порт уже занят

**Решение:**
- Измените порты в `docker-compose.yml`
- Или остановите другие сервисы, использующие те же порты

## 📝 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

[Укажите авторов проекта]

## 🤝 Вклад в проект

[Укажите информацию о том, как можно внести вклад в проект]

---

**Примечание:** Этот проект создан для демонстрации архитектуры микросервиса рассылок. Для продакшена рекомендуется добавить:
- Аутентификацию и авторизацию (JWT)
- Rate limiting
- Логирование (структурированные логи)
- Мониторинг и алертинг (Prometheus, Grafana)
- Тесты (unit, integration)
- CI/CD пайплайны
- Обработку ошибок и retry-логику
- Валидацию входных данных
- Документацию API (OpenAPI)

