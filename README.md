![Tests](https://github.com/Babylon14/notification-service/actions/workflows/tests.yml/badge.svg)

# Notification Service (Сервис рассылок)

Сервис для управления контактами и массовой рассылки email-уведомлений с асинхронной обработкой задач через Celery.

## 📋 Описание проекта

Notification Service — это REST API сервис на базе FastAPI, предназначенный для:
- Управления базой контактов (создание, просмотр, поиск, удаление)
- Создания и отправки массовых email-рассылок
- Асинхронной обработки задач рассылки через Celery
- Аутентификации пользователей с использованием JWT токенов

Сервис использует асинхронную архитектуру для обеспечения высокой производительности при обработке большого количества email-рассылок.

## 🛠 Стек технологий

### Backend Framework
- **FastAPI** (0.128.0) — асинхронный веб-фреймворк для Python
- **Uvicorn** (0.40.0) — ASGI сервер для запуска FastAPI приложения

### База данных
- **PostgreSQL** (16-alpine) — реляционная БД
- **SQLAlchemy** (2.0.45) — асинхронный ORM для работы с БД
- **Alembic** (1.17.2) — система миграций базы данных
- **asyncpg** (0.31.0) — асинхронный драйвер для PostgreSQL

### Асинхронные задачи
- **Celery** (5.6.2) — распределенная очередь задач
- **Redis** (7-alpine) — брокер сообщений для Celery
- **Flower** (2.0.1) — веб-интерфейс для мониторинга Celery

### Безопасность и аутентификация
- **python-jose[cryptography]** (3.5.0) — работа с JWT токенами
- **passlib** (1.7.4) — хеширование паролей (bcrypt)

### Email
- **aiosmtplib** (5.0.0) — асинхронная отправка email через SMTP

### Валидация данных
- **Pydantic** (2.12.5) — валидация и сериализация данных
- **email-validator** (2.3.0) — валидация email адресов

### Тестирование
- **pytest** (9.0.2) — фреймворк для тестирования
- **pytest-asyncio** (1.3.0) — поддержка асинхронных тестов
- **httpx** (0.28.1) — HTTP клиент для тестов

### Инфраструктура
- **Docker** & **Docker Compose** — контейнеризация приложения
- **PgAdmin4** — веб-интерфейс для управления PostgreSQL

### Дополнительные библиотеки
- **Jinja2** (3.1.6) — шаблонизатор для HTML писем
- **python-dotenv** — загрузка переменных окружения

## 📁 Структура проекта

```
notification-service/
├── alembic/                    # Миграции базы данных
│   ├── versions/              # Файлы миграций
│   └── env.py                 # Конфигурация Alembic
├── src/
│   ├── api/                   # API эндпоинты
│   │   ├── api_auth.py        # Авторизация (регистрация, вход)
│   │   ├── api_contacts.py    # Управление контактами
│   │   ├── api_mailings.py    # Управление рассылками
│   │   └── dependencies.py    # Зависимости (JWT аутентификация)
│   ├── core/                  # Основные настройки
│   │   ├── config.py          # Конфигурация приложения
│   │   └── security.py        # Безопасность (хеширование, JWT)
│   ├── models/                 # SQLAlchemy модели
│   │   ├── user.py            # Модель пользователя
│   │   ├── contact.py         # Модель контакта
│   │   └── mailing.py         # Модель рассылки
│   ├── schemas/                # Pydantic схемы
│   │   ├── schema_user.py     # Схемы пользователя
│   │   ├── schema_contact.py  # Схемы контакта
│   │   └── schema_mailing.py  # Схемы рассылки
│   ├── tasks/                  # Celery задачи
│   │   ├── celery_app.py      # Конфигурация Celery
│   │   └── mailing_tasks.py   # Задачи рассылки
│   ├── database.py            # Подключение к БД
│   └── main.py                # Точка входа FastAPI
├── tests/                      # Тесты
│   ├── conftest.py            # Конфигурация pytest
│   ├── test_auth.py           # Тесты авторизации
│   └── test_mailings.py       # Тесты рассылок
├── docker-compose.yml         # Docker Compose конфигурация
├── Dockerfile                 # Docker образ приложения
├── requirements.txt           # Python зависимости
├── pyproject.toml            # Конфигурация проекта
└── alembic.ini               # Конфигурация Alembic
```

## 🎯 Функциональность

### Что реализовано:

1. **Аутентификация и авторизация**
   - Регистрация новых пользователей
   - Вход в систему с получением JWT токена
   - Защита эндпоинтов через JWT токены
   - Хеширование паролей с использованием bcrypt

2. **Управление контактами**
   - Создание контакта (email, имя, фамилия)
   - Получение списка контактов с пагинацией
   - Поиск контакта по email
   - Удаление контакта
   - Управление статусом активности контакта

3. **Рассылки**
   - Создание рассылки (требуется авторизация)
   - Автоматическая отправка email всем активным контактам
   - Отслеживание статуса рассылки:
     - `pending` — формируется
     - `processing` — в процессе отправки
     - `completed` — завершена
     - `failed` — ошибка
   - Получение списка рассылок текущего пользователя

4. **Асинхронная обработка**
   - Использование Celery для фоновой обработки задач
   - Параллельная отправка email через отдельные задачи
   - Автоматические повторы при ошибках (до 5 попыток)
   - Мониторинг задач через Flower

5. **Email отправка**
   - Отправка через SMTP (поддержка Mailtrap для тестирования)
   - HTML шаблоны для писем
   - Обработка ошибок отправки

6. **База данных**
   - Миграции через Alembic
   - Асинхронные запросы к БД
   - Связи между таблицами (User ↔ Mailing)

7. **Тестирование**
   - Unit тесты для авторизации
   - Интеграционные тесты для рассылок
   - Тестовая база данных

## 🚀 Инструкция по запуску

### Предварительные требования

- Docker и Docker Compose установлены
- Python 3.12+ (для локальной разработки)

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd notification-service
```

### Шаг 2: Создание файла `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# База данных PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=mailing_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# URL подключения к БД
DATABASE_URL=postgresql+asyncpg://postgres:your_secure_password@db:5432/mailing_db

# Redis
REDIS_URL=redis://redis:6379/0

# PgAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin_password

# SMTP настройки (для тестирования используйте Mailtrap)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_mailtrap_username
SMTP_PASSWORD=your_mailtrap_password

# JWT настройки
SECRET_KEY=your_secret_key_here_min_32_chars
ALGORITHM=HS256
```

**Важно:** Замените все значения на ваши реальные данные, особенно пароли и секретные ключи.

### Шаг 3: Запуск через Docker Compose

```bash
docker-compose up --build
```

Эта команда запустит все необходимые сервисы:
- PostgreSQL база данных (порт 5432)
- Redis (порт 6379)
- FastAPI приложение (порт 8000)
- Celery Worker
- Flower (мониторинг Celery, порт 5555)
- PgAdmin (порт 5050)

### Шаг 4: Применение миграций базы данных

В отдельном терминале выполните:

```bash
docker-compose exec app alembic upgrade head
```

Или если запускаете локально:

```bash
alembic upgrade head
```

### Шаг 5: Проверка работы

Откройте в браузере:
- **API документация (Swagger):** http://localhost:8000/docs
- **Альтернативная документация (ReDoc):** http://localhost:8000/redoc
- **Flower (мониторинг Celery):** http://localhost:5555
- **PgAdmin:** http://localhost:5050

## 📖 Использование API

### 1. Регистрация пользователя

```bash
POST /auth/register
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "secure_password"
}
```

### 2. Вход в систему

```bash
POST /auth/login
Content-Type: application/x-www-form-urlencoded

username=user@example.com&password=secure_password
```

Ответ содержит JWT токен:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

### 3. Создание контакта

```bash
POST /contacts/
Content-Type: application/json

{
  "email": "contact@example.com",
  "first_name": "Иван",
  "last_name": "Иванов",
  "is_active": true
}
```

### 4. Получение списка контактов

```bash
GET /contacts/?limit=10&offset=0
```

### 5. Поиск контакта по email

```bash
GET /contacts/search?email=contact@example.com
```

### 6. Удаление контакта

```bash
DELETE /contacts/{contact_id}
```

### 7. Создание рассылки (требуется авторизация)

```bash
POST /mailings/
Authorization: Bearer {your_jwt_token}
Content-Type: application/json

{
  "subject": "Новости компании",
  "content": "Добро пожаловать в нашу рассылку!"
}
```

### 8. Получение своих рассылок (требуется авторизация)

```bash
GET /mailings/me
Authorization: Bearer {your_jwt_token}
```

## 🧪 Запуск тестов

Для запуска тестов:

```bash
# Через Docker
docker-compose exec app pytest

# Или локально (если установлены зависимости)
pytest
```

## 🔧 Локальная разработка (без Docker)

Если вы хотите запустить проект локально без Docker:

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Убедитесь, что PostgreSQL и Redis запущены локально

3. Обновите `.env` файл с локальными настройками:
```env
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/mailing_db
REDIS_URL=redis://localhost:6379/0
```

4. Примените миграции:
```bash
alembic upgrade head
```

5. Запустите FastAPI:
```bash
uvicorn src.main:app --reload
```

6. В отдельном терминале запустите Celery Worker:
```bash
celery -A src.tasks.celery_app worker --loglevel=info
```

7. (Опционально) Запустите Flower:
```bash
celery --broker=redis://localhost:6379/0 flower
```

## 📊 Модели данных

### User (Пользователь)
- `id` — уникальный идентификатор
- `email` — email адрес (уникальный)
- `hash_password` — хешированный пароль
- `is_active` — статус активности

### Contact (Контакт)
- `id` — уникальный идентификатор
- `email` — email адрес (уникальный)
- `first_name` — имя (опционально)
- `last_name` — фамилия (опционально)
- `is_active` — статус активности
- `created_at` — дата создания

### Mailing (Рассылка)
- `id` — уникальный идентификатор
- `subject` — тема письма
- `content` — содержимое письма
- `status` — статус рассылки (pending, processing, completed, failed)
- `created_at` — дата создания
- `owner_id` — ID пользователя-создателя (внешний ключ)

## 🔐 Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- Защита эндпоинтов через OAuth2 Bearer токены
- Валидация всех входных данных через Pydantic
- Переменные окружения для чувствительных данных

## 📝 Миграции базы данных

Создание новой миграции:
```bash
alembic revision --autogenerate -m "описание изменений"
```

Применение миграций:
```bash
alembic upgrade head
```

Откат миграции:
```bash
alembic downgrade -1
```

## 🐳 Docker команды

Остановка всех контейнеров:
```bash
docker-compose down
```

Остановка с удалением volumes:
```bash
docker-compose down -v
```

Просмотр логов:
```bash
docker-compose logs -f app
docker-compose logs -f worker
```

Перезапуск конкретного сервиса:
```bash
docker-compose restart app
```

## 📚 Дополнительная информация

- **API документация:** Доступна автоматически по адресу `/docs` (Swagger UI)
- **Мониторинг задач:** Flower доступен на порту 5555
- **Управление БД:** PgAdmin доступен на порту 5050
- **Логи:** Все логи выводятся в консоль контейнеров

**Версия:** 0.1.0  
**Python:** 3.12+  
**Последнее обновление:** 2025
